# Spring Boot configuration

spring:

  # Database
  datasource:
    url: jdbc:postgresql://localhost:5433/shop_db?currentSchema=shop
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 080801

  # Flyway
  flyway:
    schemas: shop

  #Timer
  application:
    logback:
      name: main-logback

    notification:
      enabled: true #true/false
      sms:
        time: 0 * * ? * *
        prefix: SMS || -
      mail:
        time: 0 * * ? * *
        prefix: Mail || -

      send:
        orders-unpaid:
          enabled: true
          how:
        happy-birthday:
          enabled: true
          how:
        orders-paid:
          enabled: true
          how:
        message:
          enabled: true
          recipient: 89278911111
          text: hello all!
          how: sms mail






