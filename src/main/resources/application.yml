# Spring Boot configuration

spring:
  jpa:
    open-in-view: false

  # Database
  datasource:
    url: jdbc:postgresql://localhost:5433/shop_db?currentSchema=shop
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 080801

  # Flyway
  flyway:
    schemas: shop

  #Timer
  application:
    logback:
      name: main-logback

    notification:
      enabled: true #true/false
      poolSize: 5
      split: _
      sms:
        time: 0 * * ? * *
        prefix: SMS || -
      mail:
        time: 0 * * ? * *
        prefix: Mail || -
      social:
        time: 0 * * ? * *
        prefix: ВКОНТАКТЕ || -
      send:
        orders-unpaid:
          enabled: true
          how: mail_sms_sms_social_social_mail
        happy-birthday:
          enabled: true
          how: mail_sms_sms_social_social_mail
        orders-paid:
          enabled: true
          how: mail_sms_sms_social_social_mail
        message:
          enabled: true
          recipient: 89278911111
          text: hello !
          how: mail_sms_sms_social_social_mail






